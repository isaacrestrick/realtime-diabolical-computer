# Copy to `backend/.env` and fill in.
OPENAI_API_KEY=

# Optional: comma-separated list of allowed CORS origins for local dev
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional: override where the computer-use-demo UI is running.
# COMPUTER_DEMO_ORIGIN=http://localhost:8080

# Optional: explicitly target a docker container ID/name for Opus tasks.
# COMPUTER_USE_DEMO_CONTAINER=
